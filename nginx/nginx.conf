events { }
http {
	 
    sendfile on;
 
 
    upstream docker-grafana {
        server grafana:3000;
    }

    server {
	listen 8081;

        location / {
            proxy_pass http://docker-grafana;	
	}	
    }	
}
